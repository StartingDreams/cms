  - name: shared
    source: ./
    persistentVolumeName: site-volume
    persistentVolumeAccessModes: ReadOnlyMany
    destination: /home/core/shared
    nfs: true
    mount_options: 'nolock,vers=3,udp,noatime'
